[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_18", "nodePackages.npm"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm ci"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "gunicorn -w 2 -b 0.0.0.0:$PORT --timeout 120 XGBOost:app"
